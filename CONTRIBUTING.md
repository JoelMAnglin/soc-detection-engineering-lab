# Contributing

Create a branch, add positive and benign tests for detection changes, run `python -m unittest discover -s tests -v`, validate the live container, and explain tuning/security impact in the pull request. Never submit real credentials, internal asset data, proprietary vendor exports, malware, or customer information.

